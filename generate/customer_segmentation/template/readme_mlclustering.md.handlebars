# readme_mlclustering.md

An example clustering project. Runs K-Means clustering on a small sample of the 
HMDA loan approval data set.

Full example of the workflow is here: src/clustering_{{fileName}}.py

Run the example: `python -m src.clustering_{{fileName}}`

Unit tests have been added as well, use `pytest` to execute these tests.
